#include "AllianceRightData.h"

void AllianceRightData::loadData(const QList<D2OField*> &fields, I18nFile *I18n)
{
  SocialRightData::loadData(fields, I18n);
  
  foreach (D2OField *field, fields)
  {
  }
}

